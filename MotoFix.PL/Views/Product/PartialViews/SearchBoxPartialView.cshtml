@model string
<div class="mt-3">
    <form method="get" asp-action="@ViewBag.ActionName" class="d-flex flex-wrap justify-content-center">
        <input name="SearchValue" value="@Model" class="form-control" placeholder="ابحث عن المنتج">
        <input class="btn btn-outline-success fs-5 mt-3" type="submit" value="بحث">
    </form>
</div>
